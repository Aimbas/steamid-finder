/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export", 
    basePath: "/steamid-finder",
    images: {
        unoptimized: true,
      },
  };

export default nextConfig;

